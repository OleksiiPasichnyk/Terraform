apiVersion: v1
kind: Service
metadata:
  name: jenkins-oleksii-oleksii
spec:
  selector:
    app: jenkins-oleksii
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jenkins-oleksii
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-oleksii
  template:
    metadata:
      labels:
        app: jenkins-oleksii
    spec:
      containers:
      - name: jenkins
        image: jenkins/jenkins:lts
        resources:
          requests:
            memory: "8000M"
            cpu: "3500m"
          limits:
            memory: "16000M"
            cpu: "4000m"
        ports:
        - containerPort: 8080